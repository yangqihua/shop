function clk(dd) {
    var parent = document.getElementById(dd);
   // parent.className = parent.className + " newDiv";  //添加classname
   parent.className = "newDivliuwei";  //改变classname
}

function clk1(dd) {
    var parent = document.getElementById(dd);
    //parent.className = parent.className + " newDiv";  //添加classname
   parent.className = "";  //改变classname
}
